"""이 프로그램의 버전 하나만 두는 곳.

**배포할 때 고치는 파일이 여기 하나가 되도록 떼어 두었다.** 예전에는
`TVerDownloader.py` 맨 위 상수였는데, 그 파일은 창과 시작 절차가 함께 든
곳이라 버전을 올리려면 700줄짜리 안에서 그 한 줄을 찾아야 했다.

**아무것도 import 하지 않는 것이 이 파일의 요점이다.** 빌드 스크립트나 검사가
버전만 알고 싶을 때 PyQt6와 앱 전체를 함께 끌어오지 않아도 된다. 앞으로도
여기에 다른 것을 들이지 않는다.

값은 `유의적 버전`(major.minor.patch)을 따른다. 업데이트 확인이 GitHub 릴리스
태그(`vX.Y.Z`)와 이 값을 숫자로 견주므로(`src/updater.py`의 `_norm`), 세 자리
숫자에서 벗어나면 새 버전이 나와도 알아보지 못한다.
"""

APP_VERSION = "3.4.0"
