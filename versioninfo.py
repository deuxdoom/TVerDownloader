"""이 프로그램의 버전 하나만 두는 곳.

**아무것도 import 하지 않는 것이 이 파일의 요점이다** - 빌드 스크립트나 검사가 버전만
알고 싶을 때 PyQt6와 앱 전체를 함께 끌어오지 않아도 된다.

세 자리(major.minor.patch)를 벗어나면 안 된다. 업데이트 확인이 릴리스 태그와 이 값을
숫자로 견주므로(src/updater.py의 _norm) 새 버전이 나와도 알아보지 못한다.
"""

APP_VERSION = "4.0.0"
