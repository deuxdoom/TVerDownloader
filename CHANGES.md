# TVerDownloader v3.1.0 Release Notes

## 3.1.0 Change

### 새 기능

- **드래그 앤 드롭** — 브라우저 주소창에서 TVer 주소를 창에 끌어다 놓으면 입력창에 들어간다. 여러 개를 한 번에 놓으면 다중 추가 창이 그 목록으로 채워진 채 열린다.
- **클립보드 자동 인식** (설정 > 일반, 기본 꺼짐) — 복사한 TVer 주소를 입력창에 넣어 준다. 다운로드가 저절로 시작되지는 않는다.
- **주소 연속 인식** — 입력창에 이미 주소가 있는데 다른 주소를 복사하면 둘을 모아 다중 추가 창을 연다. 창이 열려 있는 동안에는 복사할 때마다 목록에 한 줄씩 쌓인다.
- **키보드 단축키** — `Ctrl+,` 설정 열기, `Ctrl+L` 로그 지우기, `Del` 목록에서 선택 항목 삭제, `Esc` 검색어 지우기.
- **단축키 사용자 지정** — 설정에 `단축키` 탭 추가. 원하는 키를 직접 눌러 바꾸고, 칸을 비우면 사용하지 않는다. 겹치는 조합은 그 자리에서 알리고 저장을 막는다. 기본값 되돌리기 제공.
- **로그 패널 접기/펴기** — 다운로드 목록 우측 끝 아이콘 버튼으로 여닫고, 그 선택을 설정에 남겨 다음 실행에도 유지한다.
- **선택 항목 취소** — 진행 중인 항목은 중지하고 대기 중인 항목은 대기열에서 뺀다. 여러 개를 골라 한 번에 처리할 수 있고, 우클릭 메뉴에서도 같은 동작을 부른다.
- **영상 파일에 썸네일 포함** (설정 > 고급) — 탐색기와 외부 플레이어에서도 미리보기가 보인다.
- **음성 없음 감지** — 다운로드가 끝나면 음성 트랙을 확인해, 영상만 남은 경우 `음성 없음`으로 표시하고 재다운로드 대상으로 둔다. 저장 경로가 길어 음성 병합이 실패했을 가능성을 원인으로 함께 안내한다.
- **즐겨찾기 2열 카드** — 한 열로 세워 오른쪽이 비던 자리를 2열 그리드로 바꿨다. 창이 좁아지면 자동으로 1열로 돌아간다.
- **즐겨찾기 신규 회차 선택** — 새 영상이 2개를 초과하면 다운로드 대기열에 바로 넣지 않고 선택 창을 띄운다. 수십 개가 말없이 쏟아져 정작 지금 받고 싶은 영상이 뒤로 밀리는 것을 막는다.

### 기능 변경

- 로그 패널을 **고정 폭**으로 바꾸고(크기 조절 splitter 제거) 폭을 480px → 390px로 줄였다. 로그를 편 상태의 창 최소 폭도 1060px → 970px로 함께 내려, 같은 창 크기에서 다운로드 목록에 돌아가는 폭이 넓어졌다.
- **시리즈 분석 우선순위** — 사용자가 방금 넣은 주소를 배경으로 도는 즐겨찾기 자동 확인보다 앞에 세운다. 즐겨찾기를 여러 개 확인하는 중에도 붙여넣은 시리즈가 바로 분석된다.
- **즐겨찾기 추가 속도** — 새로 등록할 때는 제목만 확인한다. 72화 시리즈 기준 60.9초 → 4.9초.
- **GitHub API 호출 한도 처리** — yt-dlp·FFmpeg 갱신과 업데이트 확인이 한도 초과를 네트워크 오류와 구분해 안내하고, 풀릴 시각을 함께 알린다. 서버 오류일 때만 지수 백오프로 최대 3회 재시도한다.
- 자막을 **영상에 포함**하는 설정의 기본값을 끔으로 바꿨다.
- 다운로드 목록의 `Del` 키 처리를 단축키 체계로 옮겨, 조합을 바꾸면 바뀐 대로 동작한다.
- 글자를 입력하는 동안에는 수식키(Ctrl·Alt) 없는 창 단축키가 자동으로 꺼진다. 기능키(F1~F35)는 예외.
- 단축키 범위 구분 — `Del`은 다운로드 목록, `Esc`는 기록·즐겨찾기 검색칸에 포커스가 있을 때만 동작한다.
- 긴 제목을 그냥 잘라 내는 대신 **말줄임표**로 줄인다.

### 버그 수정

- 자막을 **영상에 포함**으로 설정해도 영상 옆에 `.ja.vtt` 파일이 남던 문제. 
- 썸네일을 영상에 넣지 못하면(webm 등 컨테이너 제약) 영상 파일이 멀쩡한데도 **오류로 처리**되던 문제.
- 입력창에 주소가 들어 있을 때 다른 주소를 복사하면 **아무 반응이 없던** 문제.
- `ffmpeg`가 경로 중간에 들어간 설치본에서 ffprobe 경로를 잘못 만들던 문제.

### 내부

- `src/shortcuts.py` 신규 — 단축키 정의·기본값·범위·충돌 판정을 한곳에 모았다. 설정 파일 키는 `shortcuts`.
- `src/icon.py` → `src/appicon.py`로 정리(UI 내부 아이콘을 다루는 `src/icons.py`와 이름이 헷갈렸다).
- `BulkAddDialog`에 초기 목록 지정과 `set_urls()`·`append_url()` 추가. `open_bulk_add()`는 창을 실제로 띄웠는지 반환한다.
- 코드 전반의 `#` 주석을 걷어내고 "왜 이렇게 했는지"를 docstring으로 옮겼다.
- 검증 스크립트 `tools/` 신설 — 단축키·클립보드·대기열·자막·설정·API 한도·음성·썸네일.


