# TVerDownloader Release Notes

## 3.2.0 Change

### 새 기능

- **트레이에서 바로 설정 열기** — 창을 다시 꺼내지 않아도 트레이 메뉴에서 설정을 열 수 있습니다.
- **컴퓨터를 켜면 자동으로 실행** — 트레이 아이콘에서 우클릭해 `윈도우 시작 시 실행`을 체크해 두면, 다음에 컴퓨터를 켤 때 트레이로 최소화되어 실행됩니다.

### 기능 변경

- **트레이 오른쪽 클릭 메뉴를 미려하게 다듬었습니다.** 모서리가 둥글어졌고, 항목이 `티버 다운로더 열기` · `윈도우 시작 시 실행` · `GitHub 페이지` · `설정` · `프로그램 종료` 다섯 가지로 정리됐습니다. 맨 위 항목을 누르면 창이 돌아오고, `프로그램 종료`는 묻지 않고 바로 끝납니다. 다운로드 목록에서 오른쪽 클릭했을 때 나오는 메뉴도 같은 모양이 됐습니다.
- **주소가 아닌 것을 넣으면 미리 알려 줍니다.** 실수로 문장이나 낱말을 붙여넣고 다운로드를 눌러도 이제 받으러 가지 않고 알림 창을 띄웁니다. 적어 둔 내용은 지워지지 않으니 고쳐서 다시 쓰면 됩니다. 여러 개를 한 번에 넣을 때는 주소가 아닌 줄만 건너뛰고 나머지는 그대로 받습니다.
- **로그 창이 한결 조용해졌습니다.** 테마 바꾸기, 로그 창 여닫기, 영상 재생처럼 눈으로 바로 확인되는 일은 더 이상 적지 않습니다. 실제로 받고 있는 내용만 남습니다.
- **`Ctrl+L`이 로그 창을 여닫습니다.** 예전에는 로그를 지우는 단축키였습니다. 지우기는 로그 창의 `지우기` 버튼에 그대로 있습니다.

### 버그 수정

- 즐겨찾기를 자동으로 확인하는 도중에 다른 주소를 넣으면 **가끔 확인에 실패하던 문제.** 인터넷이 잠시 느려져도 몇 번 더 시도한 뒤에 판단합니다.
- 확인 창의 `예` · `아니오` 버튼이 **글보다 왼쪽으로 치우쳐 보이던 문제.**
- 다운로드를 시작할 때 나오는 구분선이 **로그 창을 넘어 두 줄로 접히던 문제.**

---

## 3.1.0 Change

### 새 기능

- **주소를 넣는 방법이 늘었습니다** — 브라우저에서 TVer 주소를 창에 끌어다 놓거나, 복사만 해 두면 입력칸에 알아서 들어갑니다(설정 > 일반, 기본 꺼짐). 여러 개를 연달아 복사하거나 한 번에 끌어다 놓으면 다중 추가 창이 그 목록으로 채워진 채 열립니다. 어느 쪽이든 다운로드가 저절로 시작되지는 않으니 확인하고 누르면 됩니다.
- **키보드 단축키** — `Ctrl+,` 설정 열기, `Ctrl+L` 로그 지우기, `Del` 목록에서 선택 항목 삭제, `Esc` 검색어 지우기. 설정의 `단축키` 탭에서 원하는 키로 바꿀 수 있고, 칸을 비워 두면 그 단축키를 쓰지 않습니다. 이미 쓰는 키와 겹치면 저장하기 전에 알려 줍니다.
- **로그 창 접기/펴기** — 다운로드 목록 오른쪽 끝 버튼으로 여닫습니다. 접어 둔 상태는 다음 실행에도 그대로입니다.
- **선택 항목 취소** — 받는 중인 것은 멈추고, 기다리는 중인 것은 목록에서 뺍니다. 여러 개를 골라 한 번에 처리할 수 있습니다.
- **영상 파일에 썸네일 포함** (설정 > 고급) — 탐색기나 다른 재생기에서도 미리보기 그림이 보입니다.
- **음성 없음 감지** — 다 받은 뒤 소리가 들어 있는지 확인해서, 화면만 있는 경우 `음성 없음`으로 표시하고 다시 받을 수 있게 남겨 둡니다. 저장 경로가 너무 길어서 생긴 문제일 수 있다는 안내도 함께 띄웁니다.
- **즐겨찾기가 한결 편해졌습니다** — 카드를 2열로 놓아 한눈에 더 많이 보이고(창이 좁아지면 1열로 돌아갑니다), 새로 등록할 때 기다리는 시간이 크게 줄었습니다(72화짜리 기준 약 61초 → 5초). 새 영상이 3개 이상 나오면 대기열에 바로 넣지 않고 받을 것을 고르는 창을 띄웁니다.

### 기능 변경

- **로그 창을 좁히고 크기 조절을 없앴습니다.** 같은 창 크기에서 다운로드 목록에 돌아가는 자리가 넓어졌습니다.
- **방금 넣은 주소를 먼저 처리합니다.** 즐겨찾기를 자동으로 확인하는 중에 시리즈를 붙여넣어도 기다리지 않고 바로 분석합니다.
- **업데이트 확인이 실패한 이유를 알려 줍니다.** 하루 사용량을 넘긴 경우와 인터넷 문제를 구분하고, 언제 다시 시도할 수 있는지 함께 안내합니다.
- 자막을 **영상에 포함**하는 설정의 기본값을 꺼짐으로 바꿨습니다.
- 글자를 입력하는 동안에는 `Del`·`Esc` 같은 단축키가 입력을 방해하지 않습니다.
- 긴 제목을 그냥 잘라 내지 않고 **말줄임표**로 줄입니다.

### 버그 수정

- 자막을 **영상에 포함**으로 설정해도 영상 옆에 자막 파일이 따로 남던 문제.
- 영상 형식에 따라 썸네일을 넣지 못하면, 영상은 멀쩡한데도 **실패로 표시되던** 문제.
- 입력칸에 주소가 들어 있을 때 다른 주소를 복사하면 **아무 반응이 없던** 문제.
- 폴더 이름에 `ffmpeg`가 들어간 곳에 설치하면 **영상 정보 확인에 실패하던** 문제.
