# 변경 내역

TVer Downloader v3.0.0 이후 버전별 변경 사항을 모아 둔 파일입니다.

작성 방식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를, 버전 번호는
[유의적 버전](https://semver.org/lang/ko/)을 따릅니다. 최신 버전이 맨 위에 오고, 각 버전은
`새 기능` · `변경` · `수정`으로 나눠 적습니다. 아직 배포하지 않은 변경은 맨 위
`배포 예정`에 모아 두었다가 배포할 때 새 버전으로 옮깁니다.

---

## [4.2.0] - 2026-09-10

### 새 기능 (Added)

- **로그 창을 접어도 경고 문구를 확인할 수 있습니다.** 일본 외 지역에서 접속했거나 지역 확인에 실패한 경우, 필수 도구 준비에 실패한 경우처럼 중요한 안내를 다운로드 목록 위에 한 줄로 보여 드립니다. 문구를 누르면 로그 창이 열려 자세한 내용을 확인할 수 있습니다. 로그를 펼치면 요약 문구는 숨겨지고, 정상 접속 안내는 경고로 표시하지 않습니다.

- **업데이트 진행 창의 제목 줄에서도 받기를 취소할 수 있습니다.** 오른쪽 위 `X`를 누르면 아래의 취소 단추와 똑같이 업데이트 파일 받기를 중단하고 창을 닫습니다.

- **다운로드 목록에서도 완료된 항목을 다시 받을 수 있습니다.** 지금까지는 목록에서 우클릭했을 때 실패하거나 취소한 항목에만 `재다운로드`가 나왔고, 정상적으로 받은 것을 다시 받으려면 기록 탭으로 옮겨 가야 했습니다. 이제 진행 중이거나 대기 중인 항목이 아니라면 목록에서 바로 `재다운로드`를 고르실 수 있습니다. 기록 탭과 달리 이미 받은 항목인지 되묻지 않습니다. 그 카드를 짚어 재다운로드를 고르신 뒤라서, 같은 것을 한 번 더 여쭤보는 셈이 되기 때문입니다.

### 변경 (Changed)

- **창 제목 표시줄을 없애고 프로그램 전체를 한 덩이로 다듬었습니다.** 윈도우가 그리던 제목 표시줄이 사라지고, 프로그램 머리글 줄이 그 자리를 대신합니다. 빈 곳을 끌면 창이 따라오고, 두 번 누르면 최대화됩니다.
  - **네 모서리가 둥글고, 창 오른쪽 아래로 옅은 그림자가 집니다.** 바탕 화면 위에 살짝 떠 있는 것처럼 보이도록 그림자를 옅게(가장 진한 곳이 18%) 두었습니다. 최대화하면 모서리가 각지고 그림자도 사라집니다 — 화면에 꽉 찬 창에는 그림자가 질 자리가 없기 때문입니다.
  - **최소화 · 최대화 · 닫기 단추가 머리글 줄 오른쪽에 들어왔습니다.** 닫기 단추만 마우스를 올렸을 때 빨갛게 물듭니다. 되돌릴 수 없는 동작이라 나머지 둘과 구별해 두었습니다.
  - **창 크기는 예전처럼 네 가장자리와 모서리를 끌어 바꿉니다.** 최대화는 작업 표시줄을 덮지 않고 그 위까지만 채웁니다.
  - **동작은 그대로입니다.** 최소화하면 지금까지처럼 트레이로 내려가고, 닫기 단추는 설정에 정해 두신 대로(트레이로 내리거나 종료) 움직입니다.

- **설정 · 정보 · 다중 추가 · 시리즈 선택 · 알림 창까지 모두 같은 모양이 되었습니다.** 썸네일 확대 창과 업데이트 진행 창에도 같은 디자인을 적용했습니다. 대화상자에도 윈도우 제목 표시줄이 사라지고 프로그램과 같은 제목 줄이 붙습니다. 둥근 모서리와 그림자도 같고, 제목 줄을 끌어 창을 옮길 수 있습니다.
  - **알림 창의 큰 아이콘을 제목 줄로 옮겼습니다.** 본문 왼쪽을 차지하던 40px 그림이 제목 옆 16px로 줄어, 창이 작아지고 글과 단추가 창 한가운데에 놓입니다. 아이콘이 있던 칸이 사라진 만큼 가운데 기준도 다시 잡았습니다.
  - **알림 창의 폭을 서로 맞췄습니다.** 지금까지는 문구 길이대로 창이 제각각이라 종료 확인과 업데이트 안내가 잇달아 떠도 다른 프로그램의 창처럼 보였습니다. 이제 짧은 문구는 같은 폭으로 서고 높이만 내용을 따르며, 번역이 긴 언어에서는 그 언어에 맞게 넓어집니다.
  - **종료 확인 창의 아이콘을 전원 모양으로 바꿨습니다.** 지금까지는 `X` 그림이었는데 제목 줄의 닫기 단추와 같은 모양이라 한 창에 X가 둘로 보였습니다.
  - **모든 창이 프로그램 한가운데에 뜹니다.** 지금까지는 종료 확인이나 알림 창이 가운데에서 벗어난 자리에 떴고, 문구가 짧은 창일수록 더 치우쳤습니다. 프로그램이 트레이에 내려가 있을 때는 예전처럼 화면 한가운데에 뜹니다.

- **VPN을 나중에 켜도 지역 안내가 따라옵니다.** 지금까지는 프로그램을 켤 때 한 번만 IP를 확인해서, 일본 IP가 아니라는 안내를 본 뒤에 VPN을 켜도 그 안내가 그대로 남아 있었습니다. 프로그램을 껐다 켜야 `현재 일본 IP 입니다`로 바뀌었습니다. 이제 네트워크가 바뀌는 것을 알아채고 그때 다시 확인하므로, VPN을 켜면 잠시 뒤(5초 내외) 안내가 저절로 새 상태로 바뀝니다. 반대로 받는 중에 VPN이 끊긴 것도 같은 방식으로 알려 드립니다.
  - **평소에는 아무것도 확인하지 않습니다.** 몇 초에 한 번씩 되풀이해 묻는 방식이 아니라, 윈도우가 알려 주는 네트워크 변경을 받아 그 순간에만 확인합니다. IP가 달라질 수 있는 때는 네트워크가 바뀔 때뿐이라 이것으로 충분하고, 그 덕분에 배경에서 도는 일이 없습니다.
  - **같은 안내를 되풀이해 적지 않습니다.** 나라가 실제로 달라졌을 때만 로그에 새 안내가 붙습니다. VPN을 켜고 끄는 사이에 통신이 잠깐 끊겨 확인이 실패하는 것은 조용히 넘어가고, 마지막으로 알아낸 나라가 그대로 남습니다.
  - **로그 창을 접어 두셨어도 목록 위 한 줄 안내에서 바뀌는 것을 보실 수 있습니다.** 빨간 경고가 떠 있다가 VPN을 켜면 그 자리가 초록으로 바뀌고, 10초 뒤에 사라져 목록에 자리를 돌려줍니다. 지금까지는 정상 상태를 이 줄에 표시하지 않아, 경고가 그냥 사라지기만 해서 왜 사라졌는지 알 수 없었습니다. 처음부터 일본 IP인 경우에는 예전처럼 아무것도 뜨지 않습니다.

- **머리글 줄 왼쪽에 프로그램 아이콘이 들어왔습니다.** 제목 표시줄이 사라지면서 그 자리에 있던 아이콘이 보이지 않게 되어, 이름 로고 앞에 같은 심벌을 두었습니다.

- **창 제목에서 버전 번호를 뺐습니다.** 제목 표시줄이 없어져 화면에는 나오지 않고 작업 표시줄과 `Alt+Tab` 목록에만 남는 자리라, 프로그램 이름만 두었습니다. 버전은 `정보` 창에서 보실 수 있습니다.

- **정보 창의 `문의하기`를 `공식 사이트`로 바꿨습니다.** 프로그램 소개 페이지(deuxdoom.github.io/TVerDownloader)로 이어집니다. 마우스를 올렸을 때 드는 색도 노란색에서 하늘색으로 바꿨습니다.

- **소개 페이지와 어울리는 색과 여백으로 화면을 다듬었습니다.** 어두운 테마에는 깊은 네이비 배경을 적용하고, 카드 제목은 더 또렷하게, 로그 배경과 테두리는 차분하게 정리했습니다. 넓은 창에서는 가장자리 여백이 늘어나고, 설정·테마·고정·정보 아이콘과 창 제어 단추 사이의 간격도 넓혔습니다.

- **다운로드 버튼을 두 테마에 맞는 파란색으로 정리했습니다.** 밝은 테마는 흰 글자가 잘 읽히는 짙은 파랑을, 어두운 테마는 같은 계열의 더 밝은 파랑을 씁니다. 마우스를 올리거나 누를 때의 색과 글자·아이콘 대비도 함께 맞췄습니다. 같은 강조 방식을 쓰는 설정 저장 등 주요 단추에도 적용됩니다.

- **다운로드 목록의 제목과 조작 단추를 정리했습니다.** 대기·진행 개수는 목록 제목 옆에 모으고, `대기열 시작`은 파란 윤곽선 단추로 바꿨습니다. URL 입력칸과 다중 추가·다운로드 단추는 같은 높이로 맞췄습니다.

- **작은 창에서도 버튼 문구가 잘리지 않습니다.** 다운로드·기록·즐겨찾기 도구 줄은 현재 언어와 창 폭에 맞춰 자동으로 줄을 나눕니다. URL 입력과 추가처럼 함께 쓰는 조작은 같은 묶음으로 이동합니다.

- **창을 좁히면 로그가 다운로드 목록 아래로 이동합니다.** 다시 넓히면 오른쪽으로 돌아가고, 두 영역의 목록 상단도 나란히 맞춰집니다. 아래에 놓인 로그는 높이를 제한해 다운로드 목록을 볼 공간을 남깁니다. 로그를 접어 두셨다면 창 크기를 바꿔도 접힌 상태를 유지합니다.

- **메인 창을 이전보다 작게 줄일 수 있습니다.** 기본 내용 크기인 1100×700은 유지하면서 최소 내용 크기를 로그 표시 시 710×460, 숨김 시 680×460으로 낮췄습니다. 수치는 그림자 여백을 제외한 크기이며, 처음 열 때는 화면의 작업 영역에 맞춰 크기와 위치를 잡습니다.

- **설정 창의 본문을 스크롤할 수 있습니다.** 화면 배율이 높거나 작업 영역이 작아도 저장과 나가기 단추가 아래에 남아 있습니다. 긴 체크 항목은 줄을 나누어 표시합니다.

- **README와 소개 페이지의 프로그램 이미지를 새 UI로 갱신했습니다.** 다운로드 화면의 두 테마와 기록·즐겨찾기·설정·정보 화면을 반영했습니다.

### 수정 (Fixed)

- **다운로드 목록에서 썸네일을 눌러도 확대 창이 뜨지 않던 문제를 고쳤습니다.** 이번 판에서 확대 창에도 새 제목 줄을 붙이면서 테마 이름을 넘기도록 바꿨는데, 카드가 그 이름을 들고 있지 않아 창이 열리는 대신 오류가 났습니다. 이제 밝은 테마와 어두운 테마 모두에서 정상적으로 열리고, 창을 열어 둔 채 테마를 바꾸셔도 다음 확대 창부터 바뀐 테마를 따릅니다.

- **메인 창의 설정 버튼 등에 남던 강조 테두리를 없앴습니다.** 상단 아이콘 단추에 초점이 있어도 파란 테두리가 남지 않도록 이전 표시 방식으로 되돌렸습니다.

- **테마를 바꾸면 이미 기록된 로그 안내의 색도 함께 바뀝니다.** 이전 테마의 경고·성공 안내 색이 그대로 남아 새 배경에서 읽기 어려워지던 문제를 고쳤습니다. 로그 내용과 선택한 글자는 유지됩니다.

- **로그 안내 제목의 구분선 길이를 표시 폭에 맞췄습니다.** 로그가 목록 아래로 이동해 폭이 넓어진 경우에도 새로 표시하는 안내 제목이 알맞은 길이로 나옵니다.

- **낮은 창에서 빈 목록 안내가 잘리던 문제를 고쳤습니다.** 공간이 부족하면 아이콘과 긴 설명을 접고 상태 제목을 남깁니다. 여러 줄로 표시되는 제목과 긴 번역문도 필요한 높이를 확보합니다.

[4.2.0 코드 수정 내역](https://github.com/deuxdoom/TVerDownloader/compare/v4.1.0...v4.2.0)

---

## [4.1.0] - 2026-09-09

### 새 기능 (Added)

- **기록 탭에서 목록 끝까지 내리면 이전 기록이 이어서 나옵니다.** 지금까지는 최근 30개만 보이고 그보다 오래된 것은 검색으로만 찾을 수 있었습니다. 이제 끝까지 내릴 때마다 **15개씩 더 불러와 최대 120개까지** 이어집니다.
  - **더 있다는 것을 목록 끝에서 알려 드립니다.** 남은 개수와 함께 `아래로 내리면 이어서 나옵니다`라고 적혀 있고, 120개를 다 채우면 그때부터는 예전처럼 `검색하여 찾을 수 있습니다`로 바뀝니다.
  - **불러올 때 목록이 맨 위로 튀지 않습니다.** 보고 계시던 자리에 그대로 이어 붙으므로, 더 보려고 내려온 자리를 잃지 않습니다.
  - **한 번에 120개를 그리지 않는 데에는 이유가 있습니다.** 목록이 느려지는 주된 원인은 카드 한 장을 만드는 값이라, 처음부터 전부 그리면 기록 탭을 열 때마다 그만큼 기다리게 됩니다. 끝까지 내려 보시는 분만 그 값을 나눠 내도록 했습니다.
  - **펼쳐 두신 만큼은 그대로 남습니다.** 다운로드가 끝나 목록이 새로 그려져도 접히지 않고, 검색어나 정렬을 바꾸실 때만 처음 30개로 돌아갑니다.

### 변경 (Changed)

- **프로그램 아이콘을 하나의 디자인으로 통일했습니다.** 실행 파일·창·트레이·프로그램 정보·README에 서로 다른 그림이 나오던 것을, `T`와 `V`를 푸른 리본이 접힌 형태로 결합한 심벌로 바꿨습니다. 배경 없이 심벌 자체의 윤곽이 드러나도록 만들었습니다.

- **받는 동안 트레이 아이콘이 아래에서 위로 차오릅니다.** 지금까지는 아이콘 둘레에 진행률 고리를 둘렀는데, 고리가 들어갈 자리를 내주느라 **아이콘 자체가 절반 크기로 줄어들어** 무슨 그림인지 알아보기 어려웠습니다. 이제 고리를 없애고 아이콘을 평소 크기 그대로 두면서, 심벌 모양을 따라 초록색이 아래에서부터 차오릅니다.
  - **받는 동안에는 심벌이 회색으로 바뀝니다.** 그래야 어디까지 찼는지가 한눈에 들어옵니다. 다 받으면 원래 파란 아이콘으로 돌아갑니다.
  - **밝은 작업 표시줄과 어두운 작업 표시줄 어느 쪽에서도 보이는 색으로 골랐습니다.** 흰색이 어두운 표시줄에서는 가장 선명하지만 밝은 표시줄에서는 아이콘이 통째로 사라져 쓰지 않았습니다.

### 수정 (Fixed)

- **업데이트를 받는 도중에 `Esc`를 누르면 작업이 끝나기 전에 정리가 시작되던 문제** 창을 3초만 기다렸다가 닫아 버려서, 서버 응답이 늦어 파일을 아직 쓰고 있을 때(최대 30초) 그 폴더를 지우려 들었습니다. 이제 `Esc`와 창 닫기도 `취소` 단추와 똑같이 동작합니다 — 받기를 멈추고 `취소 중`을 보여 드린 뒤, 실제로 멈춘 것을 확인하고 창을 닫습니다.

- **업데이트 파일에서 폴더 밖을 가리키는 항목을 걸러 냅니다** 내려받은 압축 파일이 정상인지 확인할 때 손상 여부만 보고 **각 항목이 어디에 풀리는지는 보지 않았습니다.** 이제 프로그램 폴더 바깥을 가리키는 항목이 하나라도 있으면 그 파일을 통째로 거부합니다. 정상적으로 배포된 파일에서는 일어나지 않는 일이며, 파일이 중간에 바뀌었을 경우를 위한 안전장치입니다.

- **같은 영상인데도 주소 뒤가 다르면 중복으로 받던 문제** TVer 링크를 공유하시면 `?utm_source=...`처럼 꼬리가 붙는 경우가 있는데, 프로그램이 주소를 글자 그대로 비교해 **같은 회차를 대기열에 두 번 넣고 이미 받으신 것도 다시 받았습니다.** 만들어지는 파일 이름은 같아서, 두 다운로드가 한 파일을 동시에 쓸 위험도 있었습니다. 이제 TVer 주소는 종류와 ID만 보고 같은 영상인지 가립니다.
  - **TVer가 아닌 주소는 손대지 않습니다.** 유튜브처럼 주소 뒤쪽에 영상 번호가 들어가는 곳이 있어, 그것을 떼면 아예 다른 영상을 받게 되기 때문입니다.
  - **이전 버전에서 쌓인 기록도 함께 찾습니다.** 예전에 꼬리가 붙은 주소로 저장된 기록이 있으면 프로그램을 켤 때 같은 회차끼리 한 줄로 모읍니다. 같은 회차가 여러 줄이면 가장 최근 것을 남깁니다.

- **확인 중에 지운 즐겨찾기가 되살아나던 문제** 즐겨찾기 시리즈를 확인하는 데에는 회차가 많으면 1분 가까이 걸리는데, 그 사이에 해당 시리즈를 지우셔도 **확인이 끝나면 목록에 다시 나타났습니다.** `모두 갱신` 중이었다면 거기서 그치지 않고 **지우신 시리즈의 새 회차가 다운로드 대기열에 들어가기까지 했습니다.** 이제 결과를 반영하기 전에 그 즐겨찾기가 아직 있는지 확인하고, 없으면 조용히 넘어갑니다.

- **설정을 저장하다 막히면 설정이 통째로 초기화되던 문제** 설정 파일에 곧바로 덮어쓰는 방식이라, 쓰는 도중에 막히면 **내용이 잘린 파일이 그대로 남았습니다.** 다음에 켜실 때 프로그램이 그 파일을 읽지 못해 저장 폴더 · 화질 · 단축키가 한꺼번에 기본값으로 돌아갔습니다. 이제 임시 파일에 다 쓴 뒤 바꿔치기하므로, 막히더라도 쓰시던 설정이 그대로 남습니다.
  - **저장에 실패하면 설정 창이 닫히지 않습니다.** 지금까지는 실패를 알리는 창이 뜬 뒤에도 설정 창이 닫혀, 프로그램이 저장된 것처럼 계속 동작했습니다. 이제 창이 그대로 남아 다시 시도하실 수 있습니다.
  - **저장에 실패하면 실행 중인 설정도 바뀌지 않습니다.** 예전에는 파일에 쓰기 전에 프로그램 안의 설정을 먼저 바꿔서, 저장이 실패한 뒤 창을 닫으시면 **파일과 다른 값이 그 실행에서만 적용된 채로 남았습니다.** 이제 저장에 성공한 뒤에 반영합니다.

- **오래 켜 두시면 메모리 사용량이 계속 늘어나던 문제** 다운로드가 끝날 때마다 영상 정보를 기억해 두었는데, 그것을 지우는 자리가 **재인코딩이 끝나는 순간 하나뿐**이었습니다. 기본 설정인 `원본 유지`에서는 재인코딩을 하지 않으므로 그 자리에 닿지 않아, 받으신 것이 쌓일수록 기억해 둔 정보도 함께 쌓였습니다. 목록에서 카드를 지우셔도 남았습니다. 이제 실제로 재인코딩할 항목만 기억해 둡니다.

- **FFmpeg 업데이트가 중간에 막히면 쓰던 FFmpeg까지 사라지던 문제** 새 FFmpeg를 넣기 전에 **기존 `ffmpeg.exe`와 `ffprobe.exe`를 먼저 지웠기 때문에**, 그 뒤 파일을 옮기는 단계에서 백신이 새 파일을 검사하느라 붙잡고 있으면 두 파일이 모두 없어졌습니다. 그러면 그 실행에서는 재인코딩도 음성 확인도 할 수 없었습니다.
  - **이제 지우지 않고 곧바로 덮어씁니다.** 옮기다 막혀도 쓰던 파일이 그대로 남고, 버전 기록을 남기지 않으므로 다음에 켜실 때 자동으로 다시 시도합니다.

- **재인코딩이 동시에 여러 개 돌아 컴퓨터가 느려지던 문제** 선호 코덱을 `AVC`나 `HEVC`로 바꿔 두시면 다운로드가 끝날 때마다 재인코딩이 시작되는데, **그 개수를 아무도 세지 않았습니다.** 재인코딩은 다운로드보다 대개 몇 배 오래 걸리므로 끝나는 속도가 시작되는 속도를 따라가지 못했고, FFmpeg가 계속 늘어나 CPU를 나눠 쓰다가 컴퓨터 전체가 느려졌습니다. 동시 다운로드를 5개로 두고 오래 걸리는 영상을 여럿 받으시면 서른 개까지도 겹쳐 돌 수 있었습니다.
  - **이제 재인코딩은 한 번에 하나씩만 진행합니다.** 나머지는 차례를 기다렸다가 앞의 것이 끝나면 이어서 시작합니다. 하나가 이미 CPU의 모든 코어를 쓰기 때문에, 여러 개를 겹쳐 돌려도 전체 시간은 줄지 않고 서로 느려지기만 합니다.
  - **다운로드는 그동안에도 계속됩니다.** 다운로드는 인터넷 속도에, 재인코딩은 CPU 성능에 달려 있어 서로 막지 않습니다.
  - 차례를 기다리는 항목도 목록에서 `변환 중`으로 보이며, 프로그램을 끄시면 다음 실행에서 다른 대기 항목과 함께 되살아납니다.

- **영상 정보를 확인하는 동안 중지를 눌러도 다운로드가 시작되던 문제** 받기를 시작하면 프로그램은 먼저 yt-dlp에게 제목과 미리보기 그림을 물어보는데, 그 사이에 `중지`를 누르셔도 **확인이 끝나면 취소하신 것을 보지 않고 그대로 다운로드를 시작했습니다.** 그 뒤 목록에서는 `취소됨`으로 바뀌지만 yt-dlp는 뒤에서 계속 돌아, 지우신 항목의 파일이 조용히 만들어지기도 했습니다.
  - **중지를 누르시면 확인 작업도 함께 끊습니다.** 지금까지는 확인이 스스로 끝날 때까지(최대 60초) 기다렸습니다. 응답하지 않는 서버를 상대로 재어 보면 중지를 누르신 뒤 빠져나오기까지 **73초에서 19초로** 줄었습니다.
  - **중지와 시작이 겹치는 순간도 막았습니다.** 아주 짧은 사이지만 중지를 누르신 직후에 yt-dlp가 뜨면 그대로 끝까지 받았습니다.

- **프로그램이 준비를 마치기 전에 종료하면 남아 있던 대기열이 사라지던 문제** 프로그램을 켜면 먼저 yt-dlp와 FFmpeg를 확인하는데, 지난번에 받다 만 대기열을 목록에 되살리는 것은 그 확인이 **끝난 뒤**였습니다. 그래서 확인이 도는 동안 프로그램을 끄시면, 아직 아무것도 되살리지 않은 빈 목록이 그대로 `queue.json`에 저장되어 **지난 대기열이 통째로 지워졌습니다.** 확인에 실패한 뒤 끄셨을 때도 마찬가지였습니다.
  - **시작 프로그램으로 등록해 두신 경우에 특히 자주 겪으셨을 것입니다.** 로그인과 함께 프로그램이 트레이에 뜨는데, 그때 바로 종료하시면 늘 이 상태였습니다.
  - **이제 대기열을 프로그램이 켜지자마자 되살립니다.** 준비가 끝나기를 기다리지 않으므로 언제 끄셔도 목록이 남습니다. 되살린 항목이 저절로 받기 시작하지 않는 것은 예전과 같습니다 — `대기열 시작`을 누르셔야 시작하며, 그 단추는 준비가 끝나기 전까지 흐리게 표시됩니다.

- **번역 파일을 고치신 뒤 프로그램이 아예 켜지지 않던 문제** `lang` 폴더의 `.ini` 파일을 메모장에서 `ANSI`로 저장하시면 프로그램이 그 파일을 읽다가 그대로 멈췄습니다. **지금 쓰고 계신 언어가 아닌 파일이어도 마찬가지였습니다** — 프로그램은 시작할 때 `lang` 폴더의 파일을 전부 읽어 보기 때문입니다. 이제 읽을 수 없는 파일은 건너뛰고 나머지 언어로 정상적으로 켜집니다.
  - **`UTF-8 (BOM)`으로 저장하신 파일도 이제 제대로 읽습니다.** 지금까지는 이렇게 저장하시면 프로그램이 켜지기는 하되 그 파일의 번역이 통째로 무시되어, 고치신 내용이 반영되지 않았습니다.
  - **번역 파일은 `UTF-8`로 저장해 주십시오.** `ANSI`는 한국어·일본어·중국어 같은 글자를 담지 못해 다른 컴퓨터에서 깨집니다. 메모장에서는 `다른 이름으로 저장` 창 아래쪽의 `인코딩`에서 고르실 수 있습니다.

- **번역 파일의 중괄호 짝이 맞지 않으면 그 문구가 나오는 화면이 멈추던 문제** `{name}`처럼 프로그램이 값을 채워 넣는 자리를 고치시다가 `{`를 하나 빠뜨리시면, 그 문구를 쓰는 창이 뜨지 않았습니다. 이제 이런 경우에는 값을 채우지 않은 원문을 그대로 보여 드립니다.

- **시리즈 이름이 없는 영상을 받으면 지정하신 폴더가 아니라 드라이브 최상위에 저장되던 문제** 파일명 설정에서 `시리즈`를 켜 두시면 저장 경로가 `시리즈이름/파일이름` 모양이 되는데, 영상 정보에 시리즈 이름이 아예 없으면 앞부분이 비어 `/파일이름`만 남았습니다. 윈도우는 이렇게 `/`로 시작하는 경로를 **그 드라이브의 최상위**로 읽기 때문에, 다운로드 폴더를 `F:\영상\TVer`로 지정해 두셨더라도 파일이 `F:\` 바로 아래에 떨어졌습니다.
  - **눈치채기 어려운 문제였습니다.** 다운로드는 정상으로 끝나고 로그에도 오류가 남지 않아, 폴더를 열어 보시고 나서야 파일이 없다는 것을 아셨을 것입니다. 여러 편을 받으시면 그만큼 드라이브 최상위가 영상 파일로 찼습니다.
  - **주로 TVer가 아닌 곳의 영상에서 나타났습니다.** TVer 방송분에는 시리즈 이름이 들어 있지만, 유튜브 등의 단일 영상에는 시리즈에 해당하는 정보가 없습니다.
  - **이제 시리즈 이름이 없으면 폴더를 만들지 않고 지정하신 폴더에 바로 저장합니다.** 함께, 어떤 이름이 들어오더라도 저장 경로가 지정하신 폴더를 벗어나지 못하도록 막았습니다.

- **마우스 휠을 한 번 굴릴 때 목록이 화면째 넘어가 그 사이 항목이 보이지 않던 문제** 다운로드 · 기록 · 즐겨찾기 세 목록 모두 해당합니다. 지금까지는 휠을 굴리면 **카드 단위로** 건너뛰었는데, 그 개수를 윈도우의 `한 번에 스크롤할 줄의 수` 설정이 정했습니다. 그 값을 5로 두신 분의 화면에서 재어 보면 휠 한 칸에 **520px**이 지나가는데 화면에 보이는 것은 **476px**뿐이라, 굴리는 순간 보고 있던 것이 통째로 사라지고 그 사이 항목은 눈에 닿지도 않았습니다. 기록이 30개나 있어도 여섯 번이면 끝까지 내려가 **열 개 남짓만 있는 것처럼 느껴졌습니다.**
  - **이제 윈도우 설정과 관계없이 휠 한 칸에 화면의 35%만 움직입니다.** 지나간 항목이 화면에 겹쳐 남아 흐름이 끊기지 않습니다. 휠 설정을 1줄로 두셨든 30줄로 두셨든 같은 만큼 움직입니다.
  - **터치패드로 쓸어 넘기는 것과 `Ctrl`을 누른 채 굴리는 것은 예전 그대로입니다.** 터치패드는 이미 손가락을 따라 부드럽게 움직이므로 손대지 않았습니다.

[4.1.0 코드 수정 내역](https://github.com/deuxdoom/TVerDownloader/compare/v4.0.0...v4.1.0)

---

## [4.0.0] - 2026-09-06

### 새 기능 (Added)

- **한국어 · 일본어 · 영어 · 중국어(간체) · 중국어(번체) · 스페인어 · 태국어를 지원합니다.** 지금까지는 헤더의 로고 그림만 윈도우 표시 언어를 따라 바뀌고 버튼 · 메뉴 · 탭 · 설정을 비롯한 나머지 문구는 전부 한국어로 고정되어 있었습니다. 윈도우 표시 언어를 영어로 두고 켜면 로고만 영어이고 화면은 한국어인 상태가 되던 것을 바로잡았습니다.
  - **`설정 > 일반` 맨 위에서 직접 고를 수 있습니다.** 기본값인 `자동 감지 (시스템 언어)`는 윈도우 표시 언어를 따라가고, 지원하지 않는 언어이면 영어로 나옵니다. 목록에 나오는 언어 이름은 `한국어` · `日本語` · `English` · `简体中文` · `繁體中文` · `Español` · `ภาษาไทย`처럼 **그 언어의 제 글자로** 적혀 있어, 지금 화면이 어떤 언어이든 찾으려는 언어를 알아볼 수 있습니다.
  - **중국어는 간체와 번체를 따로 고를 수 있습니다.** 윈도우 표시 언어가 대만 · 홍콩 · 마카오이면 `자동 감지`가 번체를, 중국 본토 · 싱가포르이면 간체를 고릅니다. 번체 쪽은 글자만 바꾼 것이 아니라 대만에서 실제로 쓰는 낱말로 옮겼습니다(`資料夾` · `剪貼簿` · `快取` · `快速鍵`).
  - **헤더의 로고 그림은 한국어 · 일본어 · 영어 세 가지뿐이고, 나머지 언어는 영어 로고가 나옵니다.** 로고는 글자를 그려 넣은 그림이라 언어마다 새로 만들어야 하는데, 번역 파일만 넣으면 언어가 늘어나는 구조와는 맞지 않기 때문입니다.
  - **언어를 바꾸고 저장하면 다시 시작할지 물어봅니다.** `지금 다시 시작`을 누르면 프로그램이 스스로 닫히고 새 언어로 다시 뜹니다. 이때 **대기열에 담아 둔 항목은 그대로 남습니다.** 지금 하지 않겠다면 창을 닫으면 되고, 바꾼 언어는 다음에 켤 때 반영됩니다.
  - **번역 파일을 직접 고칠 수 있습니다.** 프로그램 폴더 안 `_internal\lang`에 `korean.ini` · `japanese.ini` · `english.ini` · `chinese.ini` · `chinese_traditional.ini` · `spanish.ini` · `thai.ini`가 들어 있습니다. 따로 내려받으실 것은 없습니다. 메모장으로 열어 문구를 고치면 다음 실행부터 그대로 나옵니다. 파일을 하나 더 만들어 **새 언어를 직접 추가할 수도 있습니다.** 그 언어를 쓰는 윈도우에서는 `자동 감지`만으로도 새로 넣은 언어가 잡힙니다.
  - **고치신 내용을 업데이트 뒤에도 남기려면 실행 파일이 있는 곳에 `lang` 폴더를 만들어 두십시오.** `_internal\lang`에서 바로 고쳐도 다음 실행부터 반영되지만, 프로그램이 스스로 업데이트할 때 `_internal` 폴더가 통째로 새것으로 바뀌면서 고치신 내용도 함께 사라집니다. 같은 파일을 `TVerDownloader.exe` 옆 `lang` 폴더에 넣어 두시면 프로그램이 그쪽을 먼저 읽으므로 업데이트해도 그대로 남습니다.
  - **새 버전에서 문구가 늘어도 빈칸이 생기지 않습니다.** 고쳐 두신 파일에 새 항목이 없으면 그 자리만 새 버전이 들고 온 번역으로 채워집니다. 파일이 깨지거나 사라져도 프로그램은 그대로 뜹니다.
  - **만드는 방법은 실행 파일 옆 `도움말(README).txt`에 정리해 두었습니다.** 한국어 안내가 먼저 나오고 같은 내용이 영어로 이어집니다. 어느 폴더를 먼저 읽는지, 파일 이름은 어떻게 짓는지, 맨 위 `[meta]`의 `code` · `display_name` · `author`에 무엇을 적는지, 그리고 `{자리표시자}`와 여러 줄 값처럼 건드리면 안 되는 것이 무엇인지를 담았습니다. **번역하신 분의 이름을 남기는 `author` 항목이 새로 생겼습니다** — 화면에 나오지는 않고, 파일을 여는 사람이 보게 됩니다.
  - **컴퓨터를 옮기실 때 번역은 따로 챙기지 않으셔도 됩니다.** 프로그램 안에 들어 있어 함께 따라옵니다. 다만 **위처럼 `lang` 폴더를 만들어 쓰고 계셨다면** 그 폴더는 직접 옮기셔야 합니다. 빠뜨리면 고치신 문구가 기본값으로 돌아가고, 직접 넣으신 언어를 쓰고 계셨다면 윈도우 표시 언어로 뜹니다. 그 ini 파일을 도로 넣으면 원래 언어로 돌아오는데, **그 전에 설정 창에서 `설정 저장`을 누르시면 언어가 `자동 감지`로 바뀝니다.** 그렇게 되었다면 파일을 넣으신 뒤 설정에서 언어를 다시 골라 주십시오.
  - **로그 패널도 함께 번역됩니다.** 버튼과 메뉴만 바뀌고 로그는 한국어로 남으면 반쪽이라, 준비 과정 · 대기열 · 다운로드 · 변환 · 시리즈 분석 · 업데이트까지 로그에 나오는 모든 줄이 고른 언어로 나옵니다. 지역 안내에 나오는 나라 이름도 마찬가지입니다.
  - **`지금 업데이트`를 눌렀을 때 뜨는 검은 창의 안내도 번역됩니다.** 새 버전으로 갈아 끼우는 동안 `프로그램이 닫히기를 기다리는 중입니다...`처럼 진행 상황을 알려 주는 그 창입니다. 프로그램은 영어로 쓰는데 그 창만 한국어로 뜨던 것을 바로잡았습니다. 받은 파일이 깨져서 업데이트를 시작하지 못했을 때 나오는 안내도 함께 번역됩니다.
  - **카드에 적히는 재생 시간과 그 밖의 짧은 문구까지 옮겼습니다.** `45초` · `12분` 같은 재생 시간, 제목을 가져오지 못한 항목의 `(제목 없음)`, 더 이상 지원하지 않는 설정값을 바꿔 쓸 때 알리는 줄, 그리고 예기치 못한 오류로 프로그램이 멈췄을 때 뜨는 창이 여기 해당합니다.

### 변경 (Changed)

- **여러 화면의 문구를 서로 같은 표현으로 통일했습니다.** 같은 일을 가리키는 말이 화면마다 달라 헷갈리던 것을 바로잡았습니다.
  - 즐겨찾기 탭의 `갱신` 버튼이 `모두 갱신`으로 바뀌었습니다. 즐겨찾기 우클릭의 `이 시리즈 확인`도 `이 시리즈 갱신`으로 맞췄습니다.
  - 기록 탭 우클릭의 `파일이 위치한 폴더 열기`가 다운로드 목록과 같은 `파일 위치 열기`로 바뀌었습니다.
  - 기록 탭 우클릭의 `기록에서 제거`가 정렬 옆 `삭제` 버튼과 같은 `기록에서 삭제`로 바뀌었습니다.
  - 즐겨찾기 우클릭의 `삭제`가 `즐겨찾기에서 삭제`로 구체적으로 바뀌었습니다.
- **다운로드 목록의 재생 · 폴더 아이콘에 마우스를 올리면 색이 바뀝니다.** 재생은 초록, 폴더 열기는 노랑으로 물들어 누를 수 있다는 것을 바로 알 수 있습니다.
- **기록 탭에서 검색어를 칠 때 버벅이던 것이 없어졌습니다.** 기록이 많이 쌓이신 분일수록 글자를 하나 칠 때마다 화면이 멈칫했습니다. 목록을 글자마다 다시 그리던 것을 **타자를 멈춘 뒤 한 번만** 그리도록 바꾸고, 한 번에 그리는 항목을 100개에서 30개로 줄였습니다. 기록 1,000개에 다섯 글자를 치는 경우로 재어 보면 **1,143밀리초에서 56밀리초로, 스무 배 넘게 빨라졌습니다.** 검색 대상은 예전 그대로 기록 전체이고, 30개가 넘으면 목록 끝에서 몇 개가 더 있는지 알려 드립니다.
- **프로그램이 조금 더 빨리 뜹니다.** 켤 때 번역 파일 폴더를 네 번 읽던 것을 두 번으로 줄였습니다. 창이 뜨기까지 걸리는 시간 가운데 번역을 싣는 몫이 **68밀리초에서 36밀리초로** 줄었습니다. 읽는 내용과 순서는 그대로라 번역 파일을 직접 고쳐 쓰시는 방식에는 영향이 없습니다.
- **설정 창의 드롭다운 목록이 항목 길이에 맞게 줄었습니다.** `설정 > 일반`의 `언어`와 `설정 > 화질`의 `선호 코덱` · `코덱 변환 가속` 세 곳입니다. 지금까지는 `한국어`처럼 한 단어만 적혀 있어도 목록 상자가 창 너비를 통째로 차지해 오른쪽이 넓게 비어 보였습니다. 이제 가장 긴 항목이 들어갈 만큼만 넓어지므로, 어떤 언어로 쓰시더라도 그 언어의 문구 길이에 맞춰집니다.

### 수정 (Fixed)

- **입력칸을 우클릭하면 나오는 메뉴가 설정에서 고른 언어를 따라가지 않던 문제** 주소 입력창이나 검색창에서 마우스 오른쪽 버튼을 누르면 `실행 취소` · `잘라내기` · `복사` · `붙여넣기` 메뉴가 나옵니다. 이 메뉴는 프로그램이 만드는 것이 아니라 화면을 그리는 Qt가 직접 만들어서, 지금까지는 **설정에서 고른 언어가 아니라 윈도우 표시 언어**를 따라갔습니다. 그래서 한국어 윈도우에서 언어를 `English`로 바꿔도 그 메뉴만 한국어로 남았습니다. 이제 설정에서 고른 언어로 함께 바뀝니다.
  - **중국어 · 스페인어에서 그 메뉴가 영어로 뜨던 것도 함께 고쳤습니다.** 프로그램에 한국어와 일본어 번역만 들어 있어서, 중국어나 스페인어를 고르면 그 자리만 영어가 되었습니다. 이제 영어 · 중국어(간체 · 번체) · 스페인어 번역도 함께 들어 있습니다.
  - **태국어를 고르면 이 메뉴만 영어로 나옵니다.** Qt가 태국어 번역을 내놓지 않아 넣을 것이 없습니다. 나머지 화면은 모두 태국어로 나옵니다.
  - 예 · 아니오 같은 기본 버튼 문구가 나오는 일부 알림창도 같은 이유로 함께 맞춰졌습니다.
- **알림창 열한 곳이 프로그램의 다른 창과 다른 모양으로 뜨던 문제** `선택된 항목이 없습니다` · `즐겨찾기는 최대 20개까지` · `유효한 TVer 시리즈 URL을 입력하세요` 같은 알림과, 설정 창의 `썸네일 캐시 삭제` 결과 · `단축키 충돌` · `설정 저장 실패`, 그리고 프로그램을 켤 때 준비에 실패하면 뜨는 창입니다. 지금까지는 윈도우 기본 아이콘과 기본 버튼으로 떠서, 프로그램의 다른 확인창과 나란히 놓으면 다른 앱처럼 보였습니다. **버튼 글자도 설정에서 고른 언어를 따르지 않았습니다.** 이제 나머지 창과 같은 아이콘 · 색 · 버튼 모양으로 통일했고, 두 테마 모두에서 같은 모습으로 나옵니다.
- **일본어 제목이 든 파일에서 오류 이유 대신 `None`만 로그에 찍히던 문제** 자막을 SRT로 바꾸다 실패했을 때와, 받아 둔 영상의 코덱을 확인하다 실패했을 때입니다. 프로그램이 FFmpeg가 내놓는 글을 한국어 윈도우 기본 문자표로 읽으려 해서, 파일 이름에 일본어가 섞이는 순간 읽기가 통째로 실패했습니다. TVer 제목은 늘 일본어라 사실상 항상 그랬습니다. 이제 실제 오류 내용이 그대로 나옵니다.
- **즐겨찾기 백업 파일이 끝없이 쌓이던 문제** 즐겨찾기를 담거나 지울 때, 그리고 `모두 갱신`으로 새 회차를 확인할 때마다 `favoritbak` 폴더에 그 시점의 사본이 하나씩 남는데 오래된 것을 지우는 코드가 없었습니다. 기록 쪽 `historybak`은 원래 30개까지만 남기고 있어, 즐겨찾기만 쓸수록 파일이 계속 늘어났습니다. 이제 같은 30개까지만 남기고 오래된 것부터 지웁니다. **이미 쌓여 있던 것도 다음 저장 때 함께 정리됩니다.**
- **언어를 바꾸지 않았는데 다시 시작할지 물어보던 문제** 한국어 윈도우에서 언어를 `자동 감지`와 `한국어` 사이로 옮기면 실제로 쓰는 언어는 그대로인데도 다시 시작하겠느냐고 물었습니다. 이제 **실제로 쓰게 될 언어가 달라질 때만** 물어봅니다.
- **yt-dlp를 받다 끊기면 망가진 파일이 남던 문제** 내려받는 도중에 인터넷이 끊기거나 디스크가 차면 `bin` 폴더에 반쯤 받은 `yt-dlp.exe`가 그 이름 그대로 남았습니다. 다음에 켤 때 GitHub 확인까지 실패하면 프로그램이 그 파일을 멀쩡한 것으로 보고 넘어가, 받는 것마다 알 수 없는 이유로 실패했습니다. 이제 다 받은 뒤에 제자리로 옮기므로, 끊기면 쓰던 파일이 그대로 남고 다음 실행에서 다시 받습니다.
- **시작 프로그램으로 켜지는 중에 창이 뜰 수 있던 문제** 프로그램이 이미 떠 있는 상태에서 `--tray`로 한 번 더 실행되면, 창 없이 트레이로만 있어야 하는데도 원래 창이 앞으로 나왔습니다.
- **정보 창의 `제작자 유투브` 버튼이 `제작자 유튜브`로 바뀌었습니다.** 표기가 잘못되어 있던 것을 바로잡았습니다.
- **로고가 없는 언어를 골랐을 때 헤더가 그림 대신 글자로 나오던 문제** 로고 그림은 한국어 · 일본어 · 영어 세 가지뿐이라, 그 밖의 언어에서는 헤더만 밋밋한 글자 제목이 되어 그 언어에서만 프로그램 모양이 달라 보였습니다. 이제 영어 로고 그림이 나옵니다.
- **직접 추가한 언어를 `자동 감지`가 알아보지 못하던 문제** 자동 감지가 한국어 · 일본어만 알아보도록 코드에 박혀 있어서, `lang` 폴더에 중국어 파일을 넣어도 중국어 윈도우에서 영어로 떴습니다. 이제 윈도우 표시 언어를 `lang` 폴더에 실제로 들어 있는 파일과 맞춰 봅니다. **번역 파일 하나만 넣으면 그 언어가 그대로 살아납니다.**
- **`다운로드` · `기록` · `즐겨찾기` 탭 오른쪽의 빈자리에서 마우스 커서가 손 모양으로 바뀌던 문제** 세 탭은 왼쪽에만 모여 있고 그 오른쪽은 아무것도 없는 빈 줄인데, 거기에 마우스를 올려도 누를 것이 있는 것처럼 손 모양이 되었습니다(실제로 눌러도 아무 일이 없었습니다). 이제 탭 위에 있을 때만 손 모양이 됩니다.
- **창 위아래의 왼쪽 여백이 조금씩 어긋나 있던 문제** 주소 입력창은 창 가장자리에서 16px, 그 아래의 탭 상자 · 목록 제목 · 목록 상자는 12px 떨어져 있어, 세로로 보면 위 두 줄만 안쪽으로 밀려 있었습니다. 4px이라 눈에 잘 띄지 않지만 창을 넓힐수록 도드라지던 것을 12px로 맞췄습니다. 오른쪽 끝(다운로드 버튼 · 로그 상자)도 함께 맞습니다.
- **화면을 그리는 내부 코드를 정리했습니다.** 카드 · 목록 · 메뉴가 한 파일에 몰려 있던 것을 성격에 따라 나누고, 기록 탭과 즐겨찾기 탭의 카드가 따로 들고 있던 같은 코드를 하나로 모았습니다. 겉으로 보이는 화면과 동작은 달라지지 않습니다.

[4.0.0 코드 수정 내역](https://github.com/deuxdoom/TVerDownloader/compare/v3.6.0...v4.0.0)

---

## [3.6.0] - 2026-08-29

### 새 기능 (Added)

- **프로그램을 켤 때 지금 IP가 일본인지 확인해서 알려 줍니다.** TVer는 일본 지역 제한이 있어 VPN을 켜고 써야 하는데, 지금까지는 VPN을 켰는지 알 방법이 없어 받기 시작해서 실패한 뒤에야 알아차릴 수 있었습니다. 이제 앱 실행 후 준비가 끝나면 `안내` 칸에 실제로 확인한 결과가 나옵니다.
  - 일본이면 **초록빛**으로 `현재 일본 IP 입니다. 원활한 다운로드가 가능합니다.`
  - 일본이 아니면 **붉은색**으로 `현재 일본 IP가 아닙니다. / 대한민국 국가이므로 VPN을 켜주세요. / TVer는 일본 지역 제한이 있어 VPN 없이는 받을 수 없습니다.` 지금 어느 나라로 잡히는지 함께 알려 주므로, VPN을 켰는데 다른 나라로 나오는 경우도 바로 알 수 있습니다.
  - **확인하지 못했으면 그 사실을 밝힙니다.** 인터넷이 끊겼거나 응답이 예전과 달라졌을 때는 `IP 확인이 실패했습니다.`와 함께 예전의 일반 안내를 그대로 보여 줍니다. **확인에 실패한 것을 '일본이 아니다'로 말하지는 않습니다.** 그렇게 하면 VPN을 켜 두신 분에게 VPN을 껐다고 알리는 셈이 되기 때문입니다.
- **기록 탭 우클릭 메뉴에 `파일이 위치한 폴더 열기`가 생겼습니다.** 받아 둔 파일이 남아 있는 항목에서만 나타나며, 다운로드 목록의 같은 메뉴와 동작이 같습니다.
- **기록 탭에서 여러 항목을 한 번에 지울 수 있습니다.** 정렬 드롭다운 왼쪽에 `삭제` 버튼이 새로 생겼습니다. 평소엔 다른 버튼과 같은 모양이다가 마우스를 올리면 빨간색으로 바뀝니다. 목록에서 골라 지울 뿐 받아 둔 파일은 그대로 남습니다.

### 변경 (Changed)

- **준비가 끝났을 때 나오던 `환경 설정 완료. 다운로드를 시작할 수 있습니다.`가 `환경 설정 완료.`로 짧아졌습니다.** 바로 위 지역 안내가 `VPN 없이는 받을 수 없습니다`라고 말한 뒤에 곧바로 `시작할 수 있습니다`가 붙으니 어느 쪽이 맞는 말인지 헷갈렸습니다. 준비가 끝난 것은 프로그램이지 다운로드가 되는 상태라는 뜻이 아니므로, 준비가 끝났다는 것까지만 말합니다.
- **로그 글자색이 지역 안내를 빼고 모두 통일되었습니다.** 지금까지는 글에 `완료`·`성공`·`[오류]`가 들어 있으면 그 줄에 색을 입혔습니다. 그런데 그 낱말은 딱히 알릴 것이 없는 줄에도 흔해서, `ffmpeg.exe 이동 완료`나 `환경 설정 완료` 같은 줄까지 색이 붙었습니다. 로그 곳곳에 색이 붙어 있으니, 정작 색으로 구분해야 할 지역 안내가 눈에 띄지 않았습니다. 이제 색이 붙는 것은 `안내` 칸 하나뿐이고, 나머지는 모두 기본 글자색입니다.
- **기록 탭 우클릭 메뉴의 `URL 복사`가 `브라우저에서 열기`로 바뀌었습니다.** 즐겨찾기 탭 메뉴와 같은 동작입니다. 주소를 복사해 브라우저에 직접 붙여 넣던 수고를 줄였습니다.

[3.6.0 코드 수정 내역](https://github.com/deuxdoom/TVerDownloader/compare/v3.5.0...v3.6.0)

---

## [3.5.0] - 2026-08-23

### 새 기능 (Added)

- **다운로드 목록에 영상 길이가 표시됩니다.** 재생·폴더 아이콘 왼쪽에 `45분`처럼 굵게 나옵니다. 받기 전에 얼마짜리인지 알 수 있어, 여러 개를 대기열에 넣어 두었을 때 어느 것이 오래 걸릴지 짐작할 수 있습니다. 1분이 안 되는 짧은 영상은 `45초`처럼 초로 적습니다. 길이를 알 수 없는 영상에서는 아무것도 표시하지 않습니다.

### 변경 (Changed)

- **버튼에 마우스를 올리면 하는 일에 따라 색이 다르게 나타납니다.** 지금까지는 즐겨찾기 `삭제`만 색이 바뀌어서, 나머지 버튼은 누르기 전에 성격을 알기 어려웠습니다. 네 가지로 나눴습니다.
  - **진한 빨강**: `선택 항목 취소`, 즐겨찾기 `삭제`. 되돌릴 수 없습니다. 받던 것을 끊으면 받다 만 파일도 함께 지워집니다.
  - **주황**: `완료 항목 삭제`. 목록에서 카드만 지울 뿐 **받아 둔 파일은 그대로 남습니다.** 같은 줄에 있는 `선택 항목 취소`와 색을 달리해, 되돌릴 수 있는 것과 없는 것을 눈으로 구별하게 했습니다.
  - **파랑**: 즐겨찾기 `추가`. 목록이 늘어납니다.
  - **초록**: 즐겨찾기 `갱신`. 등록해 둔 것을 다시 확인할 뿐 목록이 늘지는 않습니다.

  모든 버튼에 색을 넣으면 오히려 산만해지므로 잘못 누르면 곤란한 것과 자주 쓰는 것에만 넣었습니다.
- **시리즈 분석 제외 키워드가 바뀌었습니다.** 먼저 `SP`를 뺐습니다. 예고편이나 다이제스트를 걸러내려고 두었던 것인데, 드라마 스페셜 회차 제목에 `SP`가 들어가는 경우가 있어서 정작 받아야 할 본편이 목록에서 빠졌습니다. 대신 본편이 아닌 것이 확실한 `ティザー`(티저)와 `メイキング`(메이킹)을 넣었습니다. 기본값은 이제 `予告, ティザー, ダイジェスト, メイキング, ナビ, 解説放送版` 여섯입니다. `설정 > 고급`에서 언제든 고칠 수 있고, 이미 쓰고 계신 분의 설정은 그대로 유지되므로 바꾸려면 직접 손봐야 합니다.
- **TVer 주소를 복사하면 입력창에 자동으로 들어갑니다.** 기본값이 켜짐으로 바뀌었습니다. 주소를 복사한 뒤 프로그램으로 옮겨 와 붙여 넣는 과정이 한 단계 줄어듭니다. TVer 주소가 아닌 클립보드 내용에는 반응하지 않고, 다운로드가 저절로 시작되지도 않습니다. `설정 > 일반`에서 끌 수 있습니다.
- **프로그램을 켤 때 즐겨찾기 새 회차를 확인하지 않습니다.** 기본값이 꺼짐으로 바뀌었습니다. TVer는 일본 지역 제한이 있어서, VPN을 켜기 전에 확인이 돌면 등록해 둔 시리즈가 전부 실패로 끝납니다. 특히 윈도우 시작 프로그램으로 등록해 두면 늘 그 순서가 됩니다. 확인이 필요할 때는 즐겨찾기 탭의 `갱신`을 누르면 되고, 예전처럼 켤 때마다 확인하려면 `설정 > 일반`에서 켜면 됩니다.
- **파일 속성과 작업 관리자에 프로그램 정보가 나옵니다.** 지금까지는 `TVerDownloader.exe`의 속성 창을 열어도 버전 항목이 비어 있었고, 작업 관리자 `시작 프로그램` 탭에서도 `게시자`가 공란이었습니다. 서명되지 않은 프로그램은 그러지 않아도 낯설어 보이는데, 만든 사람과 버전까지 비어 있으면 알아볼 단서가 없었습니다. 이제 버전과 게시자가 함께 표시됩니다. 버전은 빌드할 때 프로그램 버전을 그대로 가져오므로 실제 버전과 어긋나지 않습니다.

### 삭제 (Removed)

- **`설정 > 고급`의 `다운로드 후 변환`을 없앴습니다.** AVI·MOV로 바꾸거나 MP3로 소리만 뽑아내는 기능이었습니다. 이 프로그램으로 주로 받는 것이 일본 버라이어티와 드라마라 소리만 남길 일이 거의 없고, AVI·MOV도 요즘 쓸 곳이 없어 켜 두는 사람이 드물었습니다. 코덱을 바꾸는 `설정 > 화질`의 `선호 코덱`은 그대로 있습니다. 예전에 이 설정을 켜 두었더라도 이제는 무시되고, 받은 파일은 MP4로 남습니다.

[3.5.0 코드 수정 내역](https://github.com/deuxdoom/TVerDownloader/compare/v3.4.0...v3.5.0)

---

## [3.4.0] - 2026-08-18

### 새 기능 (Added)

- **프로그램을 꺼도 대기열이 남습니다.** 지금까지는 다운로드를 기다리던 항목이 프로그램을 끄는 순간 함께 사라졌습니다. 카드까지 없어져서 다음에 켰을 때 무엇을 넣어 두었는지 알 방법이 없었습니다. 이제 대기 중인 항목은 물론 **받는 도중에 끊긴 항목까지** 목록에 그대로 되살아납니다. 제목과 썸네일도 함께 남아 있어 어떤 영상인지 바로 알 수 있습니다.
  - **되살아난 항목은 저절로 받기 시작하지 않습니다.** 목록에만 서 있고, 다운로드 목록 위에 생기는 `대기열 시작`을 눌러야 받습니다. TVer는 일본 지역 제한이 있어서, 윈도우 시작 프로그램으로 등록해 두면 VPN을 켜기 전에 프로그램이 먼저 뜹니다. 그 자리에서 바로 받기 시작하면 넣어 둔 것이 전부 실패로 끝나기 때문입니다.
  - 받다 만 파일은 예전처럼 지워지고, 되살아난 항목은 처음부터 다시 받습니다. `대기열 시작`을 누르기 전에 필요 없는 것은 오른쪽 클릭해 `대기열에서 제거`로 빼면 됩니다.
  - 프로그램 안에서 업데이트할 때도 그대로 남습니다. 업데이트는 프로그램을 껐다 켜는 일이라 예전에는 그때마다 대기열을 잃었습니다.
- **다운로드가 빨라졌습니다.** TVer 영상은 수백 개의 작은 조각으로 나뉘어 있는데, 지금까지는 그것을 한 개씩 차례로 받아 회선을 다 쓰지 못했습니다. 이제 여러 조각을 한꺼번에 받습니다. `설정 > 일반 > 한 영상에서 동시에 받을 조각 수`에서 조절할 수 있고 기본값은 4입니다. 동시 연결이 너무 많아지는 조합은 막습니다. 동시 다운로드 개수와 조각 수를 곱한 값이 20개를 넘으면 이유를 알려 주고 저장하지 않습니다. 연결이 너무 많으면 TVer 서버가 접속을 차단하는데, 한번 막히면 값을 되돌려도 한동안 다운로드가 되지 않기 때문입니다.

### 변경 (Changed)

- **차례를 기다리는 영상도 제목과 썸네일이 보입니다.** 동시 다운로드 개수를 넘겨 대기 중인 항목은 받기 시작할 때까지 `제목 로딩 중…`과 빈 그림으로 남아 있었습니다. 이제 기다리는 동안 미리 불러와 두므로, 무엇을 넣어 두었는지 목록만 보고 알 수 있습니다.
- **`설정 > 파일명`을 한눈에 알아보게 바꿨습니다.** `시리즈명` · `방송날짜` · `회차번호` · `타이틀` · `고유ID`에 각각 색이 붙고, 아래 미리보기의 같은 자리도 같은 색으로 나옵니다. 체크를 켜고 끌 때 파일명의 어디가 달라지는지 글을 읽지 않고 알 수 있습니다. 미리보기 예시도 한 줄에 들어가는 짧은 것으로 바꿨습니다.
- **다운로드 개수/조각 수 설정을 변경하면 두 값이 함께 로그에 남습니다.** 예시 `동시 다운로드 개수 5개 / 조각 수 4개`처럼 뜹니다. 예전에는 동시 다운로드 개수만 보였습니다.
- **설정 > 화질의 `상세 품질 설정`을 없앴습니다.** CRF·CQ 숫자를 직접 넣는 칸이 다섯 개 있었는데, 어떤 숫자가 좋은지 알려 주는 것이 아무것도 없어서 사실상 짐작으로 넣는 칸이었습니다. 이제 코덱과 CPU/GPU 조합에 따라 검증된 값을 자동으로 씁니다(AVC는 CRF 20, HEVC는 CRF 23, GPU는 각각 CQ 19·22). 화면에서 사라졌을 뿐 품질이 낮아지지는 않으며, 오히려 예전 기본값보다 좋아집니다.
- **코덱 변환 가속에서 `Intel (QSV)`과 `AMD (AMF)`를 뺐습니다.** CPU와 NVIDIA 두 가지만 남습니다. 두 선택지는 화면에 있었지만 실제로 어떤 결과가 나오는지 확인해 본 적이 없어서, 고른 사람만 아무 안내 없이 다른 품질을 받거나 변환에 실패했습니다.
- **선호 코덱에서 `VP9`와 `AV1`을 뺐습니다.** `원본 유지` · `AVC/H.264` · `HEVC/H.265` 세 가지만 남습니다. 두 코덱은 CPU로 만들면 영상 하나에 몇 시간이 걸리는 데다, 그렇게 만든 파일을 편집 도구가 대부분 읽지 못했습니다. 재인코딩을 쓰는 이유가 호환성인데 목적과 반대로 가는 선택지였습니다.
  - **예전 설정이 남아 있어도 알아서 처리합니다.** `Intel`·`AMD`를 골라 뒀다면 `CPU`로, `VP9`·`AV1`을 골라 뒀다면 `원본 유지`로 대신 진행하고, 프로그램을 켤 때 로그에 그 사실을 남깁니다. 아무 안내 없이 바뀌지는 않습니다.
- **재인코딩한 영상에 화면 색 정보와 호환성 표시를 함께 넣습니다.** 원본에 있으면 그대로 따라가고, 없으면 표준값(bt709)을 넣습니다. HEVC로 만들 때는 편집 도구와 퀵타임이 잘 읽는 형식으로 표시합니다. 재생기나 편집 도구에 따라 색이 다르게 보이던 것이 줄어듭니다.
- **변환 로그를 줄였습니다.** 코덱 확인 · 인코더 설정 · ffmpeg 명령줄 · 원본 삭제까지 한 편에 예닐곱 줄씩 남던 것을 `변환 시작` · `파일 변환 성공` 두 줄로 줄였고, 긴 파일명을 되풀이해 적지 않습니다. **변환에 실패했을 때는 예전처럼 인코더 설정과 명령줄을 모두 남깁니다.** 그때는 그 내용이 있어야 원인을 확인할 수 있기 때문입니다.

### 수정 (Fixed)

- **썸네일이 한 번 깨지면 그 카드만 계속 빈칸으로 남던 문제** 통신이 끊기거나 VPN 중간 페이지가 그림 대신 돌아와도 그것을 그대로 캐시에 저장했고, 다음부터는 파일이 있다는 이유로 다시 받지 않았습니다. `설정 > 캐시`에서 통째로 비우기 전에는 해결되지 않았습니다. 이제 그림이 맞는지 확인한 뒤에 저장하고, **이미 깨진 채로 저장되어 있던 것도 다음에 켤 때 알아서 지우고 다시 받습니다.**
- **재인코딩해도 소리는 원본 코덱 그대로 남던 문제** `설정 > 화질 > 선호 코덱`을 `AVC/H.264`로 두고 받으면 영상만 H.264로 바뀌고 소리는 원본(유튜브 영상이라면 Opus)으로 남았습니다. **Opus 소리가 든 파일은 프리미어 프로 같은 편집 도구에서 오디오 트랙이 아예 잡히지 않아**, 겉보기에는 멀쩡한 mp4가 편집에 쓸 수 없는 파일이 되었습니다. 이제 영상을 다시 만들 때 소리도 AAC로 함께 다시 만듭니다.
  - **소리 비트레이트는 원본에 맞춰 정합니다.** 96k짜리 원본을 192k로 부풀리면 용량만 늘고 음질은 그대로이기 때문입니다. 원본보다 낮아지지 않는 선에서 가장 가까운 값(96k · 128k · 160k · 192k 중 하나)을 고르고, 5.1처럼 채널이 많으면 그만큼 늘려 잡습니다.
  - **원본 소리가 이미 AAC면 아무것도 하지 않고 그대로 옮깁니다.** 다시 만들면 음질만 나빠지기 때문입니다.
  - **`원본 유지`로 두었다면 예전과 똑같이 아무것도 건드리지 않습니다.**
- **일부 영상에서 재인코딩이 실패하던 문제** 60fps 1080p 영상을 `HEVC/H.265`로 바꿀 때 변환이 오류로 끝났습니다.
- **드롭다운(선택 상자)을 펼치면 테두리가 이중으로 보이던 문제** 안쪽 목록은 모서리가 둥근데 그것을 감싸는 바깥 상자가 사각형으로 남아, 둥근 테두리 밖에 각진 테두리가 한 겹 더 보였습니다. 밝은 테마에서 특히 눈에 띄었습니다. 이제 오른쪽 클릭 메뉴와 똑같이 모서리가 둥근 모양 하나로 나오고 그림자도 지지 않습니다. `설정 > 화질`의 선호 코덱·코덱 변환 가속, `기록` 탭의 정렬 등 프로그램 안의 모든 선택 상자에 적용됩니다.
  - **펼쳐지는 방향에 따라 모양이 달라 보이던 것도 함께 고쳤습니다.** 목록이 아래로 열릴 때만 선택 상자가 살짝 비쳐 보이고 위나 가운데로 열릴 때는 멀쩡해서, 같은 선택 상자인데도 누를 때마다 모양이 달라 보였습니다. 이제 어느 방향으로 열리든 선택 상자를 정확히 덮습니다.
  - 목록에서 가리키고 있는 항목의 강조 표시도 둥근 모서리 안에 들어옵니다.
- **목록을 다시 그릴 때 썸네일이 늦게 뜨던 문제** 기록 탭은 검색어를 한 글자 칠 때마다 목록을 새로 그리는데, 사라진 카드가 요청해 둔 썸네일이 그대로 남아 자리를 차지했습니다. 특히 삭제된 영상의 썸네일은 응답이 올 때까지 그 자리를 오래 차지하고 있어서, 지금 보이는 카드의 썸네일이 그만큼 늦게 떴습니다. 이제 사라진 카드의 요청은 함께 정리합니다.

[3.4.0 코드 수정 내역](https://github.com/deuxdoom/TVerDownloader/compare/v3.3.0...v3.4.0)

---

## [3.3.0] - 2026-08-17

### 새 기능 (Added)

- **프로그램 안에서 바로 업데이트할 수 있습니다.** 새 버전이 나오면 뜨는 안내창에서 `지금 업데이트`를 누르면 됩니다. 브라우저에서 zip을 받아 직접 덮어쓸 필요가 없고, **다운로드 폴더 설정 · 받은 기록 · 즐겨찾기는 그대로 남습니다.** 옆의 `내역 확인`을 누르면 무엇이 바뀌었는지 먼저 볼 수 있고, 지금 받지 않으려면 창을 닫으면 됩니다. 업데이트 후 생기는 `update-workspace` 폴더는 이전 버전 백업이며, 새 버전이 정상적으로 실행되면 다음에 켤 때 알아서 지워집니다.
- **정보 창의 `업데이트 확인`으로 원할 때 직접 확인할 수 있습니다.** 최신이면 최신이라고 알려 줍니다. 자동 확인은 `설정 > 일반 > 업데이트`에서 꺼 둘 수 있습니다.
- **트레이 아이콘이 진행 상황을 알려 줍니다.** 창을 내려 두어도 마우스를 올리면 `3 대기 / 1 진행 · 42%`처럼 뜨고, 받는 동안 아이콘 둘레에 고리가 채워집니다.
- **다운로드 목록에서 오른쪽 클릭해 썸네일을 저장할 수 있습니다.** 파일 재생 · 위치 열기와 한자리에 모였습니다.
- **프로그램을 켤 때 즐겨찾기를 확인할지 고를 수 있습니다.** 설정은 `설정 > 일반 > 즐겨찾기`에 있습니다. VPN을 켜기 전에 확인이 돌아 등록해 둔 시리즈가 전부 실패하는 것을 막을 수 있습니다. 꺼 두어도 즐겨찾기 탭의 `갱신`은 그대로 씁니다.
- **목록이 비어 있으면 무엇을 하면 되는지 알려 줍니다.** 다운로드 · 기록 · 즐겨찾기 세 탭에 모두 적용됩니다.
- **세 탭의 이름에 마우스를 올리면 어떤 것인지 한 줄로 알려 줍니다.**

### 변경 (Changed)

- **정보 창의 링크를 버튼으로 바꿨습니다.** `제작자 유투브` · `문의하기`가 눌러도 되는 것인지 알기 어려웠는데, 이제 다른 창과 같은 버튼 모양이 되었습니다. 여기에 `업데이트 확인`이 함께 들어갔습니다.
- **내부 코드를 정리하고 자동 점검 351개를 추가했습니다.** 사용하는 데 달라지는 것은 없습니다. 코드를 고칠 때마다 예전에 겪은 문제가 되살아나지 않는지 배포 전에 확인합니다.

### 수정 (Fixed)

- **새 버전 안내창을 오른쪽 위 X로 닫을 수 없던 문제** Esc를 눌러야만 닫혔습니다.
- **프로그램을 끄거나 목록에서 지워도 영상 변환이 계속 돌던 문제** 만들다 만 영상 파일이 폴더에 남았습니다. 이제 함께 멈추고 쓰다 만 파일도 지웁니다.
- **변환을 시작하자마자 중지해도 끝까지 변환되던 문제**
- **진행률이 실제와 다르게 표시되던 문제** 50%에서 멈췄다가 갑자기 완료되거나, 자막이 있는 영상에서는 절반을 건너뛰고 받는 내내 `오디오 다운로드 중`으로 보였습니다.
- **다운로드가 끝나는 순간 프로그램이 갑자기 닫히거나, 그 뒤로 썸네일이 뜨지 않던 문제** 기록이 많을수록 자주 나타났습니다.
- **프로그램이 가끔 좌측 위 구석에 작게 뜨던 문제**
- **목록에서 항목을 고르면 둥근 카드 밖에 각진 회색 자국이 남던 문제**
- **오른쪽 클릭 메뉴의 둥근 모서리 바깥이 까맣게 보이던 문제** 밝은 테마에서 특히 눈에 띄었습니다.
- **오른쪽 클릭 메뉴의 글자가 오른쪽으로 치우쳐 보이던 문제**
- **주소 입력창의 오른쪽 클릭 메뉴에만 그림자가 지던 문제**
- **밝은 테마에서 붙여넣기 · 전체 선택 아이콘이 보이지 않던 문제**

[3.3.0 코드 수정 내역](https://github.com/deuxdoom/TVerDownloader/compare/v3.2.0...v3.3.0)

---

## [3.2.0] - 2026-08-15

### 새 기능 (Added)

- **트레이에서 바로 설정을 열 수 있습니다.** 창을 다시 꺼내지 않아도 트레이 메뉴에서 곧바로 열립니다.
- **컴퓨터를 켜면 자동으로 실행됩니다.** 트레이 아이콘에서 오른쪽 클릭해 `윈도우 시작 시 실행`을 체크해 두면, 다음에 컴퓨터를 켤 때 트레이로 최소화되어 실행됩니다.

### 변경 (Changed)

- **오른쪽 클릭 메뉴를 보기 좋게 다듬었습니다.** 모서리가 둥글어졌고, 트레이 메뉴 항목이 `티버 다운로더 열기` · `윈도우 시작 시 실행` · `GitHub 페이지` · `설정` · `프로그램 종료` 다섯 가지로 정리됐습니다. 맨 위 항목을 누르면 창이 돌아오고, `프로그램 종료`는 묻지 않고 바로 끝납니다. 다운로드 목록에서 오른쪽 클릭했을 때 나오는 메뉴도 같은 모양이 되었습니다.
- **주소가 아닌 것을 넣으면 미리 알려 줍니다.** 실수로 문장이나 낱말을 붙여넣고 다운로드를 눌러도 이제 받으러 가지 않고 알림 창을 띄웁니다. 적어 둔 내용은 지워지지 않으니 고쳐서 다시 쓰면 됩니다. 여러 개를 한 번에 넣을 때는 주소가 아닌 줄만 건너뛰고 나머지는 그대로 받습니다.
- **로그 창이 한결 간결해졌습니다.** 테마 바꾸기, 로그 창 여닫기, 영상 재생처럼 눈으로 바로 확인되는 일은 더 이상 적지 않습니다. 실제로 받고 있는 내용만 남습니다.
- **`Ctrl+L`이 로그 창을 여닫습니다.** 예전에는 로그를 지우는 단축키였습니다. 지우기는 로그 창의 `지우기` 버튼에 그대로 있습니다.

### 수정 (Fixed)

- 즐겨찾기를 자동으로 확인하는 도중에 다른 주소를 넣으면 **가끔 확인에 실패하던 문제** 이제 인터넷이 잠시 느려져도 몇 번 더 시도한 뒤에 판단합니다.
- 확인 창의 `예` · `아니오` 버튼이 **글보다 왼쪽으로 치우쳐 보이던 문제**
- 다운로드를 시작할 때 나오는 구분선이 **로그 창을 넘어 두 줄로 접히던 문제**

[3.2.0 코드 수정 내역](https://github.com/deuxdoom/TVerDownloader/compare/v3.1.0...v3.2.0)

---

## [3.1.0] - 2026-08-15

### 새 기능 (Added)

- **주소를 넣는 방법이 늘었습니다.** 브라우저에서 TVer 주소를 창에 끌어다 놓거나, 복사만 해 두면 입력칸에 알아서 들어갑니다(설정 > 일반, 기본 꺼짐). 여러 개를 연달아 복사하거나 한 번에 끌어다 놓으면 다중 추가 창이 그 목록으로 채워진 채 열립니다. 어느 쪽이든 다운로드가 저절로 시작되지는 않으니 확인하고 누르면 됩니다.
- **키보드 단축키가 생겼습니다.** `Ctrl+,`로 설정을 열고, `Ctrl+L`로 로그를 지우며, `Del`로 목록에서 선택 항목을 삭제하고, `Esc`로 검색어를 지웁니다. 설정의 `단축키` 탭에서 원하는 키로 바꿀 수 있고, 칸을 비워 두면 그 단축키를 쓰지 않습니다. 이미 쓰는 키와 겹치면 저장하기 전에 알려 줍니다.
- **로그 창을 접고 펼 수 있습니다.** 다운로드 목록 오른쪽 끝 버튼으로 여닫습니다. 접어 둔 상태는 다음 실행에도 그대로입니다.
- **선택 항목을 취소할 수 있습니다.** 받는 중인 것은 멈추고, 기다리는 중인 것은 목록에서 뺍니다. 여러 개를 골라 한 번에 처리할 수 있습니다.
- **영상 파일에 썸네일을 넣을 수 있습니다**(`설정 > 고급`). 탐색기나 다른 재생기에서도 미리보기 그림이 보입니다.
- **영상에 음성이 없는지 감지합니다.** 다 받은 뒤 소리가 들어 있는지 확인해서, 화면만 있는 경우 `음성 없음`으로 표시하고 다시 받을 수 있게 남겨 둡니다. 저장 경로가 너무 길어서 생긴 문제일 수 있다는 안내도 함께 띄웁니다.
- **즐겨찾기가 개선되었습니다.** 카드를 2열로 놓아 한눈에 더 많이 보이고(창이 좁아지면 1열로 돌아갑니다), 새로 등록할 때 기다리는 시간이 크게 줄었습니다(72화짜리 기준 약 61초 → 5초). 새 영상이 3개 이상 나오면 대기열에 바로 넣지 않고 받을 것을 고르는 창을 띄웁니다.

### 변경 (Changed)

- **로그 창을 좁히고 크기 조절을 없앴습니다.** 같은 창 크기에서 다운로드 목록이 쓰는 자리가 넓어졌습니다.
- **방금 넣은 주소를 먼저 처리합니다.** 즐겨찾기를 자동으로 확인하는 중에 시리즈를 붙여넣어도 기다리지 않고 바로 분석합니다.
- **업데이트 확인에 실패한 이유를 알려 줍니다.** 하루 사용량을 넘긴 경우와 인터넷 문제를 구분하고, 언제 다시 시도할 수 있는지 함께 안내합니다.
- 자막을 **영상에 포함**하는 설정의 기본값을 꺼짐으로 바꿨습니다.
- 글자를 입력하는 동안에는 `Del` · `Esc` 같은 단축키가 입력을 방해하지 않습니다.
- 긴 제목을 그냥 잘라 내지 않고 **말줄임표**로 줄입니다.

### 수정 (Fixed)

- 자막을 **영상에 포함**으로 설정해도 영상 옆에 자막 파일이 따로 남던 문제
- 영상 형식에 따라 썸네일을 넣지 못하면, 영상은 멀쩡한데도 **실패로 표시되던** 문제
- 입력칸에 주소가 들어 있을 때 다른 주소를 복사하면 **아무 반응이 없던** 문제
- 폴더 이름에 `ffmpeg`가 들어간 곳에 설치하면 **영상 정보 확인에 실패하던** 문제

[3.1.0 코드 수정 내역](https://github.com/deuxdoom/TVerDownloader/compare/v3.0.0...v3.1.0)

---

